# حاسبة عائد المدخرات

تطبيق ويب عربي بسيط لحساب العائد اليومي والشهري والسنوي على المدخرات، وإجمالي المبلغ بعد سنة.

## التشغيل

افتح ملف `index.html` مباشرة في المتصفح، أو شغّل خادمًا محليًا:

```bash
python3 -m http.server 8000
```

ثم افتح `http://localhost:8000`.
